export let options = {
    vus: 50,                                // Number of virtual users
    duration: '30s',                        // Duration of the test
    /*thresholds: {
        http_req_duration: ['p(95)<200'],   // Example threshold: 95% of requests should be under 200ms
    },*/
};